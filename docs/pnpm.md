# pnpm

```shell
pnpm up  --interactive --latest
# update all deps
pnpm up --latest  -r --include-workspace-root
# trst all
pnpm test -r --include-workspace-root
```


```shell
# git clean -fXd
pnpm clean
```
